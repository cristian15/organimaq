<div class="content-table">

	<%= link_to 'Volver', clientes_path, class: "btn btn-warning" %>
	<br><br>
	<% if !flash[:notice].blank? %>
     <div class = "alert alert-info">
        <%= flash[:notice] %>
     </div>
  <% end %>
	<div class="table-responsive">
		<table class="table table-striped table-hover table-bordered">
			<thead>
				<th>Fecha</th>
				<th>Lugar</th>
				<th>Descripción</th>
				<th>Valor Multa</th>
			</thead>
			
			<tbody>
				<tr>
				
					<td><%= @multa.fecha %></td>
					<td><%= @multa.lugar %></td>
					<td><%= @multa.descripcion %></td>
					<td><%= @multa.total_multa %></td>
					<td><%= button_to "Eliminar", @multa, data: {confirm: "Desea Eliminar la Multa fecha '#{@multa.fecha}'?"}, method: :delete, class: "btn btn-danger" %></td>
					<td><%= link_to "Editar", edit_multa_path(@multa), class: "btn btn-success" %></td>
					
				</tr>
			</tbody>
		</table>
	</div>

</div>