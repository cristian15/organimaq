<h1>Infractions#show</h1>
<p>Find me in app/views/infractions/show.html.erb</p>
<div class="content-table">

	<%= link_to 'Volver', operadors_path, class: "btn btn-warning" %>
	<br><br>
	<% if !flash[:notice].blank? %>
     <div class = "alert alert-info">
        <%= flash[:notice] %>
     </div>
  <% end %>
	<div class="table-responsive">
		<table class="table table-striped table-hover table-bordered">
			<thead>
				<th>Fecha</th>
				<th>Lugar</th>
				<th>Descripción</th>
				<th>Valor Multa</th>
			</thead>
			
			<tbody>
				<tr>
				
					<td><%= @infraction.fecha %></td>
					<td><%= @infraction.lugar %></td>
					<td><%= @infraction.descripcion %></td>
					<td><%= @infraction.total_multa %></td>
					<td><%= button_to "Eliminar", @infraction, data: {confirm: "Desea Eliminar la Multa fecha '#{@infraction.fecha}'?"}, method: :delete, class: "btn btn-danger" %></td>
					<td><%= link_to "Editar", edit_multa_path(@infraction), class: "btn btn-success" %></td>
					
				</tr>
			</tbody>
		</table>
	</div>

</div>