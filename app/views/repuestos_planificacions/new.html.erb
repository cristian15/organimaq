<div class="contentForm">
<legend>Ingresa Repuesto </legend>
	  <%= bootstrap_form_for(@repuestos_planificacions, remote: request.xhr?, html: { data: { modal: true } }) do |f| %>	
			
			<%= f.hidden_field :planificacion_id, value: params[:id] %>

			<%= f.collection_select :repuesto_id, Repuesto.all, :id, :nombre %>

			
			<%= f.number_field :cantidad%>
			
			  <%= f.primary 'Guardar' %>
	
	  <% end %>
<br>
	  </div>