<div class="content-table">
  <%= link_to 'Agregar +', tipo_maquinas_new_path, class: "btn btn-primary" %>
  <br><br>

  <% if !flash[:notice].blank? %>
     <div class = "alert alert-info">
        <%= flash[:notice] %>
     </div>
  <% end %>

  <div class="table-responsive">
    <table class="table table-striped table-hover ">
      <thead>
        <th>Tipo de Maquinaria</th>

      </thead>
      <tbody>
      <% @tipo_maquina.each do |tipo_maquina| %>
        <tr>
          <td><%= tipo_maquina.nombre %></td>
          <td><%= button_to "Eliminar", tipo_maquina, method: :delete, class: "btn btn-danger",  data: {confirm: "Desea Eliminar '#{tipo_maquina.nombre}'?"} %></td>
          <td><%= link_to "Editar", edit_tipo_maquina_path(tipo_maquina), class: "btn btn-success" %></td>

        </tr>
        <% end %>
      </tbody>
      
    </table>
  </div>
</div>